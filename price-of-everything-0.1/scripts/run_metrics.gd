extends Node

## Autoload: RunMetrics
## Strategy/economy outcome metrics. Observes the per-turn `turn_processed`
## summary (+ MatchState / Stockpile / LoanState / MarketState) and logs the
## financial, operational and solvency metrics used to validate whether a coded
## strategy is viable.
##
## STUB — implemented by the metrics agent. Owns: run_metrics.gd,
## tools/sim_mini_strategy.gd, stockpile.gd (capacity-loss counter only).

# Build output from the bake pipeline, regenerated by `python bake_sprite.py <building>`.
# renders/ alone runs to ~300 MB: the 1024px colour pass and shading mask for every level of
# every building. Only the FINISHED sprites, installed into the game's assets, are tracked.
renders/
exports/
